import 'package:flutter/material.dart';

class CountrySelectWidget extends StatelessWidget {
  final Future<List<Country>> countries;

  const CountrySelectWidget({required this.countries, Key? key,}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
